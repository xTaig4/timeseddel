# Sentry DSN (public identifier, not a secret). Error reporting is disabled when unset.
EXPO_PUBLIC_SENTRY_DSN=

# URL of the deployed Supabase edge function (public, no secret). The AI tab shows
# a setup notice when unset.
EXPO_PUBLIC_CHAT_URL=
